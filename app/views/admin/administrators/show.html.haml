%h1= @administrator.name
%hr
- %w( first_name last_name email city phone github linked_in twitter bio).each do |attribute|
  %p
    = attribute.split('_').join(' ').capitalize
  %p
    = @administrator.send(attribute)
  %br
%p= link_to 'Back', admin_administrators_path