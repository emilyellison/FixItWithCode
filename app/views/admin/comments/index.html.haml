%h1 
  Comments
  
%table

  %thead
    %tr
      %th Post
      %th Commenter
      %th Comment
      %th
      
  %tbody
    - @comments.each do |comment|
      %tr{ id: dom_id(comment) }
        = render 'admin/comments/comment', comment: comment