#administrators
  - @administrators.each do |admin|
    .administrator
      .administrator_pics
        = link_to '#' do
          %div{ id: admin.first_name.downcase }
          
  - @administrators.each do |admin|
    .administrator
      .administrator_text
        %div{ id: "#{admin.first_name.downcase}_profile"}
          
          %h1.name= link_to admin.name, '#'

          %h3.telephone
            = telephone_icon(30)
            %span= phone_number_link admin.phone

          %h3.email
            = email_icon(30)
            %span= mail_to admin.email, admin.email, subject: 'Hi'

          %p.pull-right.profiles
            = link_to github_icon(40), admin.github
            = link_to linked_in_icon(40), admin.linked_in
            = link_to twitter_icon(40), admin.twitter